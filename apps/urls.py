from django.urls import path
from .views import (
    LoginView,
    Login2FAVerifyView,
    Enable2FAView,
    VerifyEnable2FAView,
    Disable2FAView,
    UserListCreateView,
    UserDetailView,
    CheckUserView,
    LoanListCreateView,
    LoanDetailView,
    LoanProductListCreateView,
    LoanProductDetailView,
    RepaymentListCreateView,
    AuditLogListView,
    AdminListCreateView,
    AdminDetailView,
    AdminDeleteView,
    AdminSuspendView,
    AdminUnsuspendView,
    AdminRevokeView,
    SystemCapitalBalanceView as CapitalBalanceView,
    RegisterAdminView,
    LogoutView,
    VerifyEmailView,
    RequestPasswordResetView,
    ConfirmPasswordResetView,
    UserProfileListCreateView,
    SystemSettingsView,
    SecureSettingsView,
    SecureSettingsRevealView,
    TestMpesaConnectionView,
    TestSMSSendView,
    LoanDocumentCreateView,
    NotificationListView,
    NotificationUpdateView,
    LoanAnalyticsView,
    FinanceAnalyticsView,
    OwnerAnalyticsView,
    MpesaRepaymentView,
    MpesaDisbursementView,
    MpesaCallbackView,
    BulkSMSView,
    DirectSMSView,
    SMSLogListView,
    AdminInviteView,
    StatementUploadView,
    AssignTransactionView,
    UnmatchedTransactionsView,
    SendEmailNotificationView,
    ListEmailLogsView,
    DeactivationRequestListCreateView,
    DeactivationRequestDetailView,
    BranchListCreateView,
    BranchDetailView,
    CustomerDraftListCreateView,
    CustomerDraftDetailView,
    OwnerExistsView,
    ClaimOwnershipView,
    GodModeToggleView,
    SecurityLogsView,
    OwnerAuditListView,
    OwnerNotificationsView,
    MarkOwnerNotificationsReadView,
    OwnershipListView,
    OwnershipGrantView,
    OwnershipRelinquishView,
    OwnershipHandoverView,
    SecurityThreatsView,
    SystemHealthView,
    OwnerExportView,
    HierarchicalSecurityAlertsView,
)

from .management.staff_notification_views import (
    StaffNotificationListView,
    StaffNotificationMarkReadView,
)

urlpatterns = [
    path("team-security-alerts/", HierarchicalSecurityAlertsView.as_view(), name="team-security-alerts"),
    path("export/", OwnerExportView.as_view(), name="owner-export"),
    path("health/", SystemHealthView.as_view(), name="system-health"),
    path("auth/logout/", LogoutView.as_view(), name="logout"),
    path("auth/owner-exists/", OwnerExistsView.as_view(), name="owner-exists"),
    path("auth/claim-ownership/", ClaimOwnershipView.as_view(), name="claim-ownership"),
    path("auth/god-mode/toggle/", GodModeToggleView.as_view(), name="god-mode-toggle"),
    path("security-logs/", SecurityLogsView.as_view(), name="security-logs"),
    path("owner-audit/", OwnerAuditListView.as_view(), name="owner-audit"),
    path("owner-notifications/", OwnerNotificationsView.as_view(), name="owner-notifications"),
    path("owner-notifications/mark-read/", MarkOwnerNotificationsReadView.as_view(), name="mark-owner-notifications-read"),
    path("ownership/", OwnershipListView.as_view(), name="ownership-list"),
    path("ownership/grant/", OwnershipGrantView.as_view(), name="ownership-grant"),
    path("ownership/relinquish/", OwnershipRelinquishView.as_view(), name="ownership-relinquish"),
    path("ownership/handover/", OwnershipHandoverView.as_view(), name="ownership-handover"),
    path("branches/", BranchListCreateView.as_view(), name="branches"),
    path("branches/<str:pk>/", BranchDetailView.as_view(), name="branch-detail"),
    path(
        "notifications/send-email/",
        SendEmailNotificationView.as_view(),
        name="send-email",
    ),
    path("notifications/email-logs/", ListEmailLogsView.as_view(), name="email-logs"),
    path("auth/2fa/verify/", Login2FAVerifyView.as_view(), name="2fa-verify"),
    path("auth/2fa/enable/", Enable2FAView.as_view(), name="2fa-enable"),
    path(
        "auth/2fa/verify-enable/",
        VerifyEnable2FAView.as_view(),
        name="2fa-verify-enable",
    ),
    path("auth/2fa/disable/", Disable2FAView.as_view(), name="2fa-disable"),
    path(
        "deactivation-requests/",
        DeactivationRequestListCreateView.as_view(),
        name="deactivation-requests",
    ),
    path(
        "deactivation-requests/<str:pk>/",
        DeactivationRequestDetailView.as_view(),
        name="deactivation-request-detail",
    ),
    path("sms-logs/", SMSLogListView.as_view(), name="sms-logs"),
    path("payments/mpesa/", MpesaRepaymentView.as_view(), name="mpesa-payment"),
    path(
        "payments/disburse/", MpesaDisbursementView.as_view(), name="mpesa-disbursement"
    ),
    path("payments/callback/", MpesaCallbackView.as_view(), name="mpesa-callback"),
    path("loans/bulk-sms-defaulters/", BulkSMSView.as_view(), name="bulk-sms"),
    path("loans/direct-sms/", DirectSMSView.as_view(), name="direct-sms"),
    path("notifications/", NotificationListView.as_view(), name="notifications"),
    path(
        "notifications/<str:pk>/",
        NotificationUpdateView.as_view(),
        name="notification-detail",
    ),
    path("loans/analytics/", LoanAnalyticsView.as_view(), name="loan-analytics"),
    path(
        "finance/analytics/", FinanceAnalyticsView.as_view(), name="finance-analytics"
    ),
    path('owner/analytics/', OwnerAnalyticsView.as_view(), name='owner-analytics'),
    path("auth/login/", LoginView.as_view(), name="login"),
    path("security-threats/", SecurityThreatsView.as_view(), name="security-threats"),
    path("auth/register/", RegisterAdminView.as_view(), name="register"),
    path("auth/verify-email/", VerifyEmailView.as_view(), name="verify-email"),
    path(
        "auth/password-reset-request/",
        RequestPasswordResetView.as_view(),
        name="password-reset-request",
    ),
    path(
        "auth/password-reset-confirm/",
        ConfirmPasswordResetView.as_view(),
        name="password-reset-confirm",
    ),
    path("settings/", SystemSettingsView.as_view(), name="system-settings"),
    path("settings/secure/", SecureSettingsView.as_view(), name="secure-settings"),
    path("settings/secure/<str:key>/reveal/", SecureSettingsRevealView.as_view(), name="secure-settings-reveal"),
    path("settings/test-mpesa/", TestMpesaConnectionView.as_view(), name="test-mpesa"),
    path("settings/test-sms/", TestSMSSendView.as_view(), name="test-sms"),
    path("admins/invite/", AdminInviteView.as_view(), name="invite-admin"),
    path('repayments/upload-statement/', StatementUploadView.as_view()),
    path('repayments/assign-transaction/', AssignTransactionView.as_view()),
    path('repayments/unmatched/', UnmatchedTransactionsView.as_view()),
    path("admins/", AdminListCreateView.as_view(), name="admins"),
    path("admins/<str:pk>/", AdminDetailView.as_view(), name="admin-detail"),
    path("admins/<str:pk>/suspend/", AdminSuspendView.as_view(), name="suspend-admin"),
    path("admins/<str:pk>/unsuspend/", AdminUnsuspendView.as_view(), name="unsuspend-admin"),
    path('capital/balance/', CapitalBalanceView.as_view()),
    path("admins/<str:pk>/revoke/", AdminRevokeView.as_view(), name="revoke-admin"),
    path(
        "admins/<str:admin_id>/delete/", AdminDeleteView.as_view(), name="delete-admin"
    ),
    path("users/", UserListCreateView.as_view(), name="users"),
    path("users/check/", CheckUserView.as_view(), name="check-user"),
    path("users/drafts/", CustomerDraftListCreateView.as_view(), name="customer-drafts"),
    path("users/drafts/<str:pk>/", CustomerDraftDetailView.as_view(), name="customer-draft-detail"),
    path("users/<str:pk>/", UserDetailView.as_view(), name="user-detail"),
    path("user-profiles/", UserProfileListCreateView.as_view(), name="user-profiles"),
    path("loans/", LoanListCreateView.as_view(), name="loans"),
    path("loans/<str:pk>/", LoanDetailView.as_view(), name="loan-detail"),
    path("loan-products/", LoanProductListCreateView.as_view(), name="loan-products"),
    path(
        "loan-products/<str:pk>/",
        LoanProductDetailView.as_view(),
        name="loan-product-detail",
    ),
    path("repayments/", RepaymentListCreateView.as_view(), name="repayments"),
    path("loan-documents/", LoanDocumentCreateView.as_view(), name="loan-documents"),
    path("audit-logs/", AuditLogListView.as_view(), name="audit-logs"),
    path('staff-notifications/', StaffNotificationListView.as_view(), name='staff-notifications'),
    path('staff-notifications/mark-read/', StaffNotificationMarkReadView.as_view(), name='staff-notifications-mark-read'),
]
